# VISIONNARY — Reprise de travail (22/09/2026, mise à jour en fin de 2ᵉ session)

## Projet
- Dossier : `/Users/visionary/Downloads/Dev/Repos/VSCODE/visionnary` (app séparée de MariageAfro, **pas de dépôt git** pour l'instant)
- Stack : React 19 + Vite + TypeScript, IndexedDB (idb, base `visionnary-local` v1), PWA hors ligne (`public/sw.js`), serveur de synchro Node optionnel (`server/`, port 4311)
- Lancer : `npm run dev` → http://127.0.0.1:4730 (config `visionnary` dans `.claude/launch.json` de MariageAfro)
- Vérifier : `npm run verifier` (= `vitest run tests` + `node --test server/server.test.mjs` + `tsc -b` + `vite build`)
- Médias de test réels (non versionnés) : `test-media/` (photos mariage, vidéo MP4, vidéo iPhone HEVC, HEIC, photo lourde 21 Mo)

## Principes validés avec l'utilisateur
- But : sérénité le Jour J. Chaque plan compris en 3 secondes, rien à improviser, n'importe qui de l'équipe s'y retrouve.
- Thème **entièrement sombre** (noir, surfaces brun-charbon, accents or). Pas de page blanche. Seul le mode Terrain reste clair (plein soleil).
- Pas de longues fenêtres modales pour naviguer : écrans pleins. Les feuilles servent aux formulaires courts et aux confirmations.
- Tout en français. Tests avec de vraies photos et vidéos.
- Rien de modifié dans les données sans validation (aperçu → bouton → « Annuler » possible).

## Architecture utile
- `src/model.ts` : modules, champs, `shotSections` (chapitres du film, ordre narratif) et **helpers de chapitres** : `sectionOf` (casse tolérée, chapitres personnalisés gardés, sinon « Autres plans »), `orderSections` (ordre du film, anciens préparatifs à leur place, personnalisés ensuite), `sideOf` / `momentLabel` (côté mariée/marié d'un moment « Mariée · … »), `legacySections`.
- `src/moments.ts` : **frise d'une étape** — `stageFrise` (moments par chapitre, horaires en cascade 3 min/plan ou `shootMinutes`, horaires saisis dans `project.moments["idÉtape|chapitre"]`, pistes parallèles, dépassement), `momentState` (à faire / en cours / fait + essentiels manquants), `dayOrder` (ordre de la journée pour le Jour J).
- `src/shotlist.ts` : shot-list pro (110 plans) + `linkShotsToStages` + `proposeMoments` (rangement proposé des anciens « Préparatifs mariée / marié » par titre puis mots-clés).
- `src/features.ts` : assistant de création (options cérémonies/moments/prestations) + démo.
- `src/store.tsx` : état, sauvegarde locale, annulation (supprimer une étape supprime ses horaires de moments). `src/ui.tsx` : composants (Screen, Tabs, Sheet, Thumb — `full` = original net si ≤ 16 Mpx —, MediaViewer, MediaManager).
- `src/screens/common.tsx` : `ItemEditor`, `MediaCard` (badge d'état, focale, opérateur), `QuickView`, `mediaFor`, `operatorsOf` (couleur fixe par membre, rang dans l'équipe), `OperatorPills`, `shortFocal`.
- Écrans clés : `Shots.tsx` (Plans & scènes par chapitre), `Stage.tsx` + `stage.css` (frise par moment, route `#/etape/:id`), `MomentSplit.tsx` (rangement des anciens préparatifs), `Timeline.tsx` (déroulé/régie), `FieldMode.tsx` (Mode Jour J), `Inspirations.tsx`.

## Fait
### 1ʳᵉ session
- Refonte complète du design (maquette dark luxury), navigation simplifiée autour de la fiche tournage.
- Mode Jour J : plan courant avec référence, gros boutons FAIT/EXCELLENT/À REFAIRE/SAUTER, prises, minuteur, onglets Checklist/Rappels/Poses/Inspi.
- Galeries avec vraies vignettes vidéo (durée), aperçu rapide. Shot-list pro chargeable d'un tap, liée aux étapes.
- Préparatifs découpés en 9 moments : Mariée (Détails & accessoires, Maquillage & coiffure, Seule, Avec ses proches, Habillage) et Marié (Détails & accessoires, Seul, Avec ses proches, Habillage).

### 2ᵉ session (22/09) — les 4 points « en cours » sont faits
1. Chapitres centralisés dans `model.ts` (plus de copie dans `Shots.tsx` / `Stage.tsx`).
2. **Écran d'étape en frise par moment** : pistes « Côté mariée » / « Côté marié » côte à côte dès 768 px (empilées sur téléphone), moments communs avant/après ; pour chaque moment : horaire estimé modifiable (sélecteur natif, enregistré à la sortie du champ, lien « rétablir »), état À faire / En cours · x/y / Fait, essentiels manquants, plans en bande horizontale, bouton « + Plan » (chapitre et étape préremplis) ; alerte si une piste dépasse la fin de l'étape ; pastilles de cadreur (les horaires restent ceux de toute l'équipe).
3. **Rangement des anciens préparatifs** (`MomentSplit`, dans l'étape et dans Plans & scènes) : bandeau → aperçu groupé → « Ranger les N plans » → annulable. Les plans non reconnus restent dans leur ancien chapitre, visibles.
4. **Cartes de plan** : badge Tourné / Excellent / À refaire / Sauté, image estompée une fois tournée, cadrage · mouvement, focale, opérateur avec sa couleur ; aperçu rapide avec l'opérateur et la photo originale nette.
5. **Mode Jour J** : pastilles de cadreur (avec reste à faire), bouton « N essentiels manquants » (n'affiche plus qu'eux, un essentiel sauté y revient), MAINTENANT → ENSUITE (aperçu visuel) → PUIS, ordre de la journée (déroulé puis frise, les deux côtés croisés par horaire), grille Cadrage / Focale / Mouvement / Opérateur visible sans défiler sur un téléphone 375×812, boutons d'action plus hauts (82 px, 92 px en Terrain).
6. Tests : `tests/moments.test.ts` (14 cas, dont les 24 titres réels des anciens préparatifs). Total 35 tests métier + 3 serveur, tous verts ; build OK.

### 3ᵉ session (22/09) — refonte « Wedding Shoot Planner » (voir `CURRENT_STATE_AND_REFACTOR_PLAN.md`)
- **Phase 1** : audit + plan (`CURRENT_STATE_AND_REFACTOR_PLAN.md`), lint ESLint (`npm run lint`, inclus dans `npm run verifier`), redo global (⌘Z / ⇧⌘Z + bouton « Rétablir »), **bug de sauvegarde corrigé** (couverture exclue du ZIP et restauration impossible ; médias encore cités gardés).
- **Phase 2** : étape = **tableau de bord** (`Stage.tsx` : horaire, lieu + adresse, équipe, anneau de progression, statut, prochain plan, compteurs cliquables vidéo / photo / poses / essentiels / transitions / lumières, 14 onglets fonctionnels) ; frise extraite (`stage/Frise.tsx`), onglets dans `stage/Boards.tsx` ; type de plan Photo / Vidéo / Drone (`stageStats.ts`), codes de cadrage CU / MCU…, bibliothèque de transitions, personnes, cadence ; **ajout en masse** (`BulkAdd.tsx`, liste collée ou N plans) ; **glisser-déposer d'images qui crée plans / poses / références** (`MediaDrop.tsx`, progression avec vignettes) ; **fiche plan plein écran** (`ShotViewer.tsx` : swipe, flèches, statut À FAIRE / À REFAIRE / FAIT / ★, transition vers le plan suivant) ; transitions visibles (étiquette dorée sur les cartes, onglet dédié) ; dupliquer une étape avec ses plans (statuts remis à zéro) ; template d'étape (Presets) ; images en fondu + reflet de chargement.
- Tests : 45 métier + 3 serveur.

## À faire par l'utilisateur
- Vrai projet « Andy & Maeva » (panneau navigateur intégré, `http://localhost:4730`) : ses 24 plans de préparatifs sont encore dans l'ancien format. Ouvrir l'étape Préparatifs → bandeau → **Voir** → **Ranger les 24 plans** (aperçu vérifié : les 24 sont reconnus). Non appliqué volontairement.

## Feuille de route validée ensuite
7. Presets vraiment adaptés : cérémonies, style, équipe → préparation pertinente sans plans inutiles.
8. Repérage annotable : photo du lieu, placement caméras/couple/lumières, schéma relié aux plans (base existante : `src/Spatial.tsx`). Idée : images de référence par emplacement caméra (ex. cérémonie à 4 caméras : centre plein pied, gauche/droite champ-contrechamp poitrine, réactions public).
9. Pack individuel par cadreur (horaires, missions, références, placements), exportable et hors ligne. Base prête : `operatorsOf` + `dayOrder` filtré par cadreur.
10. Synchro d'équipe fiable : droits par rôle, modifications en attente, conflits explicites, historique.
11. Aussi demandé : plans « Same-Day Edit » marqués, pages pré-wedding dédiées.
12. Tests sur iPhone et tablette réels avec vraies images/vidéos (le service worker exige HTTPS hors localhost) — vérifier notamment la roue horaire iOS des moments.
- Idées notées : « maintenant » en surbrillance dans la frise le jour J (moment dont l'horaire contient l'heure actuelle) ; aperçus intermédiaires (~1280 px) pour les photos > 16 Mpx.

## Pièges connus
- **Deux origines dans le panneau intégré** : `http://localhost:4730` contient le vrai projet « Andy & Maeva » → lecture seule. Les tests qui écrivent se font sur `http://127.0.0.1:4730` (stockage séparé, démo « Sabrina & Daniel » + shot-list + photos de test déjà chargées).
- IndexedDB : la base s'appelle `visionnary-local` (v1). `indexedDB.open('autre-nom')` crée une base vide parasite.
- Captures du panneau navigateur parfois en retard d'un rendu (ou noires) : vérifier l'état par JS/DOM, refaire la capture.
- Pour naviguer dans le panneau : `location.hash = '/m/shots'` plutôt que l'outil navigate (rechargement → accueil).
- Erreurs « X is not defined » après édition = résidu HMR : recharger un onglet neuf avant de conclure.
- Champs horaires (`input type=time`) : l'action « type » de l'automate n'a aucun effet, utiliser des frappes `key` (« 1 0 5 5 ») puis Entrée. En émulation mobile (Android), un tap ouvre le sélecteur natif, non pilotable : tester la saisie en vue bureau.
- Upload de test : copier un fichier de `test-media/` dans `public/` le temps du test (ou `importMedia` via `await import('/src/media.ts')` en dev), puis **supprimer la copie**.
- Photos HEIC non affichables dans Chrome : repli propre avec message (voulu).


## 22/09/2026 — Repère horaire dans la frise
- Reprise depuis la conversation « Vérifier les consommations en cours » et vérification du code plus récent.
- Repère doré « Maintenant · Selon le planning » sur chaque moment actif à la date du projet, y compris les pistes parallèles et les moments qui traversent minuit. Aucun statut ni donnée modifié automatiquement.
- Actualisation toutes les 15 secondes et au retour sur la page ; nettoyage du minuteur à la fermeture.
- Vérification : 66 tests métier + 3 serveur, types et build réussis ; 10 avertissements lint préexistants, bundle principal > 500 ko.
- Chrome isolé : 375 / 768 / 1440 px sans débordement, deux moments simultanés visibles, disparition automatique à la fin. Données réelles non utilisées.


## 22/09/2026 — Galerie sobre et références animées
- Direction demandée : moins de pollution visuelle ; images prioritaires. Recherche et Importer visibles, filtres et outils secondaires repliés ; suppression du bouton Ajouter répété.
- Filtres étape/cadreur, sans affectation, compteurs contextuels, remise à zéro ; recherche multi-mots insensible aux accents couvrant cadrage, focale et affectations. Les plans archivés sont exclus dans tous les filtres.
- Bouton Animer/Figer dans Plans & scènes : vidéos muettes en boucle et originaux animés, seulement dans les cartes visibles ; désactivé par défaut, respecte prefers-reduced-motion.
- Formats MIME de repli GIF/WebP/PNG/JPEG/WebM et aperçu vidéo d'import corrigé.
- Médias Pexels et provenance dans test-media/licensed/SOURCES.md. Le MP4 est un zoom de démonstration dérivé d'une photo, pas une captation réelle.
- Démo isolée exportée dans test-media/licensed/demo-visionnary.zip ; aucun projet réel modifié. Attention : la restauration ZIP existante remplace le studio, cette archive est destinée à un navigateur/profil de test.
- npm run verifier : 67 tests métier et 3 serveur verts, types/build OK ; 10 avertissements lint antérieurs et avertissement de taille du bundle.
- scripts/qa-gallery.mjs : import de trois fichiers via interface, animation/pause, fiche, filtres et reset, responsive 375/768/1440, préférence mouvement réduit, export/restauration ZIP et persistance après rechargement. Captures docs/qa/galerie-*.png.
- Tests navigateur Chrome isolé, pas sur appareils iOS physiques.


## 22/09/2026 — Gestes du plan de scène
- Outil Déplacer la vue : glisser au-dessus des éléments sans les déplacer ; molette enfoncée également disponible.
- pointercancel annule le brouillon des éléments et du fond au lieu d'enregistrer la position interrompue.
- Après ajout sur téléphone, le panneau se replie ; élément sélectionné et bouton Régler existant accessible.
- Zone de saisie de rotation portée à 44 px sans agrandir le dessin.
- scripts/qa-scene-gestures.mjs : main au-dessus d'une caméra sans mutation, annulation du glisser, pas de débordement à 375 px. npm run verifier : 70 tests verts, types/build OK, avertissements préexistants inchangés.
- Pas de mesure de performance ni de validation tactile sur appareil réel dans ce lot.

## 22/09/2026 — Missions, angles, déplacements et alertes terrain
- Accueil orienté prochaine mission ; filtres étape/photo/vidéo/mariée/marié/ensemble et statuts immédiats avec archivage annulable.
- ReferenceGallery commune aux poses/plans : angles multiples, swipe, choix de couverture, import, légendes, vidéo en boucle ; galerie photographe avec filtre d’étape.
- Plan de scène épuré par défaut, réglages de couches visuelles, référence caméra modifiable ; moteurs de mouvements conservés.
- Trajets consécutifs : route manuelle, rangement, installation, marge, conflits et passages de minuit. Adaptateur Google Routes authentifié côté serveur, clé serveur uniquement ; aucune clé configurée ni trafic réel testé.
- Alertes optionnelles 20/10/5/0, fins d’étapes et moments, départs, rappels personnalisés ; son/vibration/notifications selon appareil. Déduplication par session, acquittement et retour de visibilité. Aucune promesse en arrière-plan ou écran verrouillé.
- Démo additive Aïcha & Malik : 18 missions, 6 étapes, 4 scènes cohérentes, 8 transitions, références Pexels créditées et clip de zoom explicitement illustratif. Aucun projet existant remplacé ; une démo ajoutée dans le navigateur intégré.
- npm run verifier : 75 tests métier + 4 tests serveur, types et build réussis ; 9 avertissements lint préexistants et avertissement de taille du bundle.
- scripts/qa-missions.mjs : démo, statuts, couverture, import, angles, scènes/champs/lecture, horaires sans conflit, responsive 375/850/768/1440, alertes/acquittement/persistance. Chrome isolé, pas de validation sur iPhone physique.
- Guide utilisateur : docs/GUIDE-TERRAIN.md. Configuration facultative : docs/GOOGLE-ROUTES.md.

## 22/09/2026 — Reprise (Claude Code) : audit visuel, contraste Terrain corrigé
- Reprise depuis une nouvelle conversation, après la session Codex du jour. Lecture de `docs/REPRISE.md` et `CURRENT_STATE_AND_REFACTOR_PLAN.md` avant toute action ; aucune écriture sur `http://localhost:4730` (vrai projet Andy & Maeva) — tout testé sur `http://127.0.0.1:4730`.
- Audit visuel des demandes de l'utilisateur (galerie structurée par moment, plans de scène avec fiche caméra/référence, accueil = déroulé du jour, mariée/marié/ensemble, like rapide, progression) : **déjà en place** — Galerie photographe = Pose Board par catégories avec favoris (cœur) ; étape Préparatifs = frise Côté mariée / Côté marié par moment ; Plans de scène = Scene Designer 2D avec cône de champ, timeline animée et fiche caméra (« Fiche caméra plein écran » : cadrage attendu, objectif, sujet — référence à épingler) ; Accueil = « Votre journée » avec filtres Tous/Mariée/Marié/Ensemble et prochaine mission.
- **Bug de contraste corrigé** : en mode Terrain (`data-mode="terrain"`, surfaces claires voulues pour le plein soleil), `.mission-shortcuts button` et `.mission-next` (`src/screens/missions.css`) posaient `background: var(--ivory)` sans `color: var(--ink)` — texte clair sur fond blanc, quasi invisible sur les deux raccourcis d'accueil (Galerie photographe / Plans de scène) et la carte « Prochaine mission ». Corrigé en appariant `color: var(--ink)` (le motif déjà utilisé par `.card`, `.next-shoot`). Vérifié en Terrain et en Studio, aucune régression.
- `npm run verifier` : 75 tests métier + 4 tests serveur, types et build toujours verts (changement CSS seul).
- Reste à auditer si temps disponible : autres paires `background: var(--ivory)` + texte doré (`.mission-travel`, bannière d'alerte terrain) — contraste à vérifier précisément en Terrain, pas confirmé cassé à l'écran.
- **Suite du même audit, même session** : le bug était plus large qu'un seul écran — chaque carte qui pose `background: var(--ivory)` (ou `--ivory-2`) sans `color: var(--ink)` assorti hérite du texte clair de la page et devient illisible dès que le mode Terrain passe l'ivoire au blanc. Repéré et corrigé sur les écrans que l'utilisateur a cités (accueil, trajets, régie, filtres des plans) :
  - Nouveau jeton `--gold-card` (`src/theme.css`) : = `--gold` en Studio/Nuit, = `#8a6018` (ambre foncé, lisible sur blanc) en Terrain. À utiliser pour tout texte doré posé sur une surface `--ivory`, jamais pour du doré sur fond sombre (qui reste `--gold`).
  - `src/screens/travel.css` (Trajets & installation) : titre de trajet et libellés horaires (`Finir les prises`, `Partir au plus tard`…) étaient illisibles — passés à `--gold-card` / `--ink-2`. `.travel-help` (hors carte, fond noir) volontairement laissé en `--text-2`.
  - `src/dayrun.css` (Régie du jour J) : horloge live, cases lever/coucher/golden hour et lignes d'étapes n'avaient aucune couleur de texte propre — ajout de `color: var(--ink)` sur `.dayrun-clock`, `.sun-grid > div`, `.dayrun-row`.
  - `src/screens/shots.css` : panneau de filtres (`.shot-filters`) sans couleur de texte — corrigé.
  - Vérifié à l'écran en Terrain sur Accueil, Trajets & installation et Déroulé · Régie ; `npm run verifier` : toujours 75 tests métier + 4 serveur, types et build verts (changements CSS uniquement).
  - **Non traité, à auditer si nécessaire** : le reste de l'app n'a pas été passé au crible composant par composant — seuls les écrans mentionnés par l'utilisateur et ceux visités pendant cette session ont été vérifiés à l'œil en Terrain. Si un autre écran affiche du texte pâle sur fond clair en Terrain, chercher le même motif (`background: var(--ivory` sans `color:` assorti dans la même règle, ou sur un ancêtre).
  - Audit refait de façon **exhaustive** (script perl parcourant tous les `.css`, règle par règle, pas seulement à l'œil) : 7 règles posent encore `background: var(--ivory`/`--ivory-2)` sans `color:` propre dans la règle elle-même (`.sheet-head`, `.thumb`, `.quickview-specs > div`, `.quickview-notes`, `.progress`, `.field-specs div`, `.split-groups section`) — vérifiées une à une via leur JSX, toutes héritent correctement `color: var(--ink)` d'un ancêtre (`.sheet`, `.field-shot`, `.card`) ou sont purement décoratives (`.thumb`, `.progress`) : rien à corriger. Aucune paire `color: var(--gold)` + `background: var(--ivory)` dans une même règle ne subsiste non plus. **L'audit contraste Terrain est maintenant complet et vérifié programmatiquement, pas seulement sur les écrans visités.**
  - Balayage automatisé des 22 routes de l'app (hash-navigation + lecture du texte affiché) : aucun écran vide ou cassé.

## 22/09/2026 — « Mes plans » : pack individuel par cadreur (feuille de route point 9)
- Carte blanche de l'utilisateur pour continuer sans validation. Repris le point 9 de la feuille de route (« Pack individuel par cadreur, exportable et hors ligne ») : jusqu'ici seul un filtre « Cadreur » existait, noyé dans les filtres de Plans & scènes — un cadreur devait fouiller 134 plans pour retrouver les siens.
- **Nouveau** : `operatorDayOrder(p, operatorId)` (`src/moments.ts`) — pure, testée (`tests/moments.test.ts`, filtre l'opérateur, exclut l'archivé, réutilise `dayOrder`). Écran `src/screens/MyPack.tsx` (route `#/pack/:operatorId`) : fiche du membre, barre de progression (X/Y tournés), ses plans uniquement groupés par étape chronologique (heure · étape · lieu/adresse), plans sans étape sous « Autres plans » (chapitre du film). Bouton imprimer/PDF réutilisant le motif `.print-report` déjà en place (`Files.tsx`) — donc utilisable hors ligne, sans dépendance réseau.
- Accès : icône caméra sur chaque ligne de « Mon équipe » → onglet Équipe (`src/screens/Team.tsx`).
- Un bug introduit puis corrigé dans la foulée : la première version posait un `<button>` dans le `<button>` de la ligne (nesting HTML invalide, erreur React) — remplacé par un `<a href="#/pack/…">`, même motif que le lien téléphone déjà présent sur la ligne. Vérifié par lecture directe du DOM après correction.
- Vérifié à l'écran (127.0.0.1:4730, projet de démo) : plans de Christopher correctement filtrés et groupés (Préparatifs 09:00 → First look 12:00 → Cérémonie 14:00 → Cocktail 17:30 → Ouverture de bal 22:30 → Autres plans), export imprimable généré avec le même contenu.
- `npm run verifier` : 76 tests métier (+1) + 4 serveur, types et build verts.
- **Pas encore fait** : bouton retour symétrique depuis « Mes plans » vers Mode Jour J filtré sur ce cadreur (actuellement Mode Jour J montre tout le monde) ; pas de test sur appareil réel.

## 22/09/2026 — Lien caméra→plan, plein écran scène, grille galerie, mise en ligne
- Suite de « carte blanche » : caméra du plan de scène → bouton « Enregistrer ce mouvement comme un plan à tourner » (`Inspector.tsx`, `SceneDesigner.onCreateShot`), crée un vrai plan lié (`shotIds`), testé bout en bout. Toggle plein écran (Fullscreen API + tentative `screen.orientation.lock('landscape')`, ignorée silencieusement sur Safari iOS qui ne l'implémente pas). Galerie photographe : grille régulière (CSS Grid, `aspect-ratio: 4/5`, `object-fit: cover`) à la place du mur façon Pinterest (columns CSS) — demande explicite de l'utilisateur, contredit un choix délibéré d'une session précédente, assumé. Visionneuse de pose : bouton « cacher les infos » pour libérer l'espace image. Photo de fond d'un plan de scène testée avec une vraie photo aérienne (fonctionnalité déjà là, jamais essayée avec une image réelle avant).
- **Mise en ligne.** Dépôt Git initialisé (n'existait pas avant cette session) et poussé sur `github.com/mariageafro/visionnary-app` (compte déjà connecté sur cette machine, confirmé avec l'utilisateur par un choix explicite — pas de nouveau compte créé). Site public : **https://mariageafro.github.io/visionnary-app/**, déploiement automatique à chaque push (`.github/workflows/deploy.yml`).
- **Chemins absolus → relatifs partout** (nécessaire pour un sous-dossier GitHub Pages) : `vite.config.ts` (`base: './'` en mode `gh-pages` uniquement, `/` en local), `main.tsx` (enregistrement du service worker), `public/manifest.webmanifest`, `public/sw.js` (réécrit pour calculer ses chemins depuis `self.registration.scope` au lieu de les coder en dur), `Welcome.tsx`, `Shoot.tsx`, `MissionBoard.tsx`, `weddingDemo.ts`. Vérifié en vrai sur le site déployé : chargement, service worker, démo « Aïcha & Malik » (photos + vidéo du dossier `public/demo-wedding/`) — tout fonctionne, capture à l'appui.
- **App bureau** : PWA installable choisie avec l'utilisateur plutôt qu'un packaging Electron/Tauri (déjà fonctionnelle, pas de nouvel outil, pas d'avertissement de signature). Rien à construire en plus : « Installer l'app » dans Chrome/Edge, ou Safari → Partager → Sur l'écran d'accueil pour iPhone/iPad — pas d'outil de transfert dédié nécessaire, c'est le mécanisme standard des PWA.
- **Limite assumée et à corriger si besoin** : le site déployé n'a QUE la démo « Aïcha & Malik » (ses médias sont dans `public/demo-wedding/`, versionnés). La démo par défaut « Sabrina & Daniel », elle-même largement illustrée cette session et la précédente, a ses médias **uniquement dans IndexedDB du navigateur de test local** (`127.0.0.1:4730`) — rien n'a été committé, donc un visiteur du site public ne les voit pas. Voir `HANDOFF-IA.md` §6.
- **Accès libre** confirmé avec l'utilisateur (pas de mot de passe) : à réserver aux données de démonstration, ne jamais y mettre de vraies informations de mariage tant qu'il n'y a pas d'authentification.

## 22/09/2026 — Rebranding, accès direct, bug de redirection, chantier serveur de synchro
- Logo « Visionary Wedding » fourni par l'utilisateur intégré (écran d'accueil). Renommage complet : titre, manifest PWA, sidebar (App.tsx ×2, Welcome.tsx), package.json, dépôt GitHub renommé `visionary-wedding` (`gh repo rename`).
- **Bug trouvé et corrigé** : configurer le domaine perso `visionarywedding.com` (CNAME) a fait rediriger le lien `mariageafro.github.io/visionary-wedding/` — qui marchait — vers ce domaine, qui affiche la page « domaine parqué » de Hostinger tant que le DNS n'est pas branché. Lien cassé un temps. Domaine retiré de la config Pages (`gh api ... -X DELETE` puis réactivation `build_type=workflow`) et fichier `public/CNAME` supprimé ; à rebrancher seulement une fois le DNS prêt (voir §DNS plus haut).
- **Accès direct au projet** : `Welcome.tsx` charge automatiquement la démo (`buildWeddingDemo`) dès l'arrivée sur le site **en production uniquement** (`import.meta.env.PROD`), sans clic — en local (`npm run dev`) le choix manuel reste, pour ne pas gêner un vrai premier tournage. Testé sur un `vite preview` (build de prod) : atterrit directement sur « Votre journée », vidéo et photos se lisent normalement (testé avec lecture effective, pas juste l'affichage).
- Demande d'une vraie collaboration d'équipe (cocher des tâches, ajouter des plans, à plusieurs, sur les 2 prochains mariages réels) : **le site statique ne peut pas faire ça** — expliqué clairement à l'utilisateur avant d'agir (chaque visiteur a sa propre copie locale). Le vrai outil pour ça existe déjà : `server/index.mjs` (compte, workspace partagé, conflit de révision géré, testé). Patché pour tourner sur un hébergeur cloud (`process.env.PORT`, gardé le garde-fou qui refuse de démarrer en réseau sans `ALLOWED_ORIGINS` et sans compte déjà créé). Reste : héberger ce serveur quelque part avec un **disque persistant** (le point critique — beaucoup d'offres gratuites remettent le disque à zéro à chaque redéploiement, inacceptable pour de vraies données de mariage) — nécessite un choix d'hébergeur et un compte que l'utilisateur doit créer lui-même. Détail complet et pourquoi dans `docs/DEPLOY-SERVEUR.md`.
- Nouveau `PROMPT-IA.txt` à la racine : prompt prêt à coller dans une autre IA (Gemini etc.), pointe vers `HANDOFF-IA.md`.
- `npm run verifier` vert (`server/server.test.mjs` aussi revérifié après le patch PORT).
- Nouveau : `HANDOFF-IA.md` à la racine — dossier de reprise condensé pour une autre IA (Gemini ou autre), pointe vers ce fichier et les autres docs plutôt que de les dupliquer.
- `npm run verifier` vert avant la mise en ligne ; build `--mode gh-pages` vérifié séparément (chemins relatifs confirmés dans `dist-ghpages/`).

## 22/09/2026 — Démo « Sabrina & Daniel » habillée de photos libres de droit
- Demande utilisateur : voir le rendu final avec de vraies images partout, pas des cases vides. 4 poses de la Galerie photographe étaient encore vides (« Portrait mariée fenêtre », « Marche du couple », « Front contre front », « Famille proche ») et les 4 caméras du plan de scène « Cérémonie religieuse » n'avaient aucune référence épinglée (pas seulement CAM C, comme aperçu en surface plus tôt dans la session — CAM A/B/D aussi).
- Sourcées sur Pexels (licence gratuite, cf. https://www.pexels.com/license/), en cohérence avec l'identité visuelle déjà choisie pour cette démo (couples noirs, mariage élégant/cinématique) et avec la mission précise de chaque caméra (ex. CAM C = « Marié côté droit : réaction à l'arrivée de la mariée » → photo d'un marié essuyant une larme). Sources consignées dans `test-media/licensed/SOURCES.md`.
- **Méthode** : rien commité dans le dépôt — importées directement dans IndexedDB du navigateur de test (`127.0.0.1:4730`) via `await import('/src/storage.ts')` + `await import('/src/media.ts')`, `fetch()` du CDN Pexels côté page, `importMedia(file, projectId, itemId)`, puis pour les caméras un patch direct de `referenceId` sur l'élément de scène suivi de `saveWorkspace`. Recharger la page après écriture directe en IndexedDB (le store React ne relit pas tout seul). Pattern déjà documenté dans les « Pièges connus » de ce fichier.
- Vérifié à l'écran : galerie complète (10/10 poses illustrées), fiche « CAM C » avec sa référence en place. `npm run verifier` toujours vert après coup (aucun fichier source touché, seulement IndexedDB + `test-media/licensed/SOURCES.md`).
- **Limite assumée** : seules la Galerie photographe et les 4 caméras de la scène « Cérémonie religieuse » ont été habillées — pas les 134 plans de « Plans & scènes » (shot-list professionnelle générique, pictogrammes voulus, pas une référence par plan) ni les autres plans de scène. À refaire à la demande si un autre écran doit être illustré.

## 22/09/2026 — Un plan illustré par chapitre, sur toute la journée
- Suite directe du point précédent : l'utilisateur a demandé un test « mariage complet » pour comparer l'ensemble habillé, en demandant explicitement des images Pinterest — **refusé** (Pinterest n'est pas une banque libre de droit, les photos y sont repostées sans licence de réutilisation ; risque de droit d'auteur). Poursuivi avec Pexels, même méthode.
- 10 plans supplémentaires illustrés, un par chapitre du film, du matin au soir : Lieu & décor, Mariée détails/coiffure/habillage, Marié habillage, Cortège, Cérémonie (entrée), Cocktail, Réception (gâteau), Ouverture de bal (première danse). Sources dans `test-media/licensed/SOURCES.md`.
- Volontairement pas exhaustif : les 134 plans ne sont pas tous illustrés (ni les chapitres Portraits mariée/marié/couple, Famille, First look, Interviews, Transitions) — un plan par chapitre suffit pour comparer le rendu habillé vs pictogramme, sans dénaturer la shot-list professionnelle générique. À la demande, la même méthode s'étend à n'importe quel autre chapitre.
- **Incident en cours de route** : le serveur de dev (port 4730) s'est arrêté seul entre deux commandes (cause non identifiée — possiblement le `npm run verifier` d'un tour précédent). Relancé via le lanceur `visionnary` ; ce lanceur ouvre par défaut `localhost:4730` (vraies données) — **rebasculé immédiatement** sur `127.0.0.1:4730` sans qu'aucune écriture n'ait eu lieu sur l'origine réelle entretemps. Vigilance à garder : après tout redémarrage de ce serveur, revérifier l'origine avant toute action.

## 22/09/2026 — Les 134 plans complétés : plus aucune case vide
- L'utilisateur a insisté : plus aucune case image vide, sur toute l'interface (« il faut complets pas de case image vide »). Passage du « un plan par chapitre » à l'exhaustif.
- **Méthode changée pour l'échelle** : au lieu d'une recherche Pexels par plan (134 recherches manuelles, intenable), une recherche large par thème/chapitre (ex. « black bride portrait garden elegant »), extraction de **tous** les liens `/photo/…-<id>/` de la page de résultats via une ligne JS (`document.querySelectorAll('a[href*="/photo/"]')`) plutôt que la lecture d'accessibilité (polluée par les liens sponsorisés iStock/Canva), puis répartition des photos récoltées sur les plans vides de ce chapitre. ~20 recherches ont fourni un vivier de 400+ photos pour couvrir 114 plans restants.
- Résultat vérifié programmatiquement : `p.items` module `shots` (134) et `poses` (10) → 0 sans média. Toutes les 4 caméras de la scène « Cérémonie religieuse » ont aussi leur référence (fait au tour précédent).
- IDs Pexels utilisés consignés par chapitre dans `test-media/licensed/SOURCES.md` (pas de commit dans le dépôt — tout en IndexedDB du navigateur de test).
- Compromis assumés à cette échelle : quelques répétitions d'image entre plans très proches, les plans « Interviews » et « vue en hauteur / drone » utilisent des photos génériques faute d'offre Pexels plus précise. Ce ne sont pas des recréations fidèles de CE mariage — des références de style/valeur de plan, comme le reste de la démo.
- `npm run verifier` revérifié après ce lot (changements IndexedDB uniquement, aucun fichier source touché).

## 22/09/2026 — Retour en arrière : abandon de « Visionary Wedding »
- L'utilisateur a fait marche arrière sur tout le rebranding précédent (« on laisse tomber Visionary Wedding.com, tu laisses comme c'était avant sur mariageafro »). Tout reverti :
- Nom et logo : `App.tsx` (×2), `Welcome.tsx` (×2, y compris l'écran de chargement auto) → « VISIONNARY / SHOOT · CREATE · EMOTION », texte redevenu comme avant l'image de logo. `index.html`, `public/manifest.webmanifest`, `package.json` → VISIONNARY. Logo fourni par l'utilisateur (`public/logo-visionary-wedding.jpg`) supprimé, plus utilisé nulle part.
- Dépôt GitHub renommé en retour : `mariageafro/visionary-wedding` → `mariageafro/visionnary-app` (`gh repo rename`), remote Git mis à jour. Site : `mariageafro.github.io/visionnary-app/`.
- Domaine personnalisé : déjà retiré au tour précédent (`cname:null` confirmé côté API Pages) — rien à refaire là-dessus, juste vérifié que ça tenait toujours.
- Références mises à jour dans `PROMPT-IA.txt` et `docs/DEPLOY-SERVEUR.md` (URL du dépôt et du site). Le reste (accès direct à la démo en production, patch serveur `$PORT`, garde-fous de sécurité réseau) **conservé** — l'utilisateur n'a demandé d'annuler que le rebranding/domaine, pas les fonctionnalités ajoutées entretemps.
- `npm run verifier` revérifié après le revert ; redéployé sur `mariageafro.github.io/visionnary-app/`, lien confirmé fonctionnel.

## 22/09/2026 — Reprise (Claude Code) : les 7 chantiers de `PROMPT-SUITE-SCENE-GALERIE.txt`
- Reprise depuis une nouvelle conversation. Lu `HANDOFF-IA.md`, `docs/REPRISE.md` et `PROMPT-SUITE-SCENE-GALERIE.txt` avant toute action ; `npm run verifier` vert en base (76 tests métier + 4 serveur) avant de commencer. Tout testé sur `http://127.0.0.1:4730` (démo « Sabrina & Daniel »), jamais sur `localhost:4730`.
- **1. Galerie photographe par sous-catégories** : `poseCategories` (`src/model.ts`) détaillé (Mariée/Marié + demoiselles/garçons d'honneur, Couple seul/et cortège, Famille côté mariée/marié/réunies, Groupe tous les invités/par table…). `PoseBoard.tsx` regroupe désormais le mur par catégorie avec un titre de section et un compteux « x/y » — même motif que les chapitres de `Shots.tsx` (`section-title`), au lieu d'un mur plat filtré par pastilles seules. Vérifié à l'écran : sections « MARIÉE SEULE », « COUPLE », « FAMILLE » avec compteurs corrects sur la démo.
- **2. Visionneuse plein écran — bouton « cacher les infos » généralisé** : ajouté à `ShotViewer.tsx` (fiche plan) et `CameraCard.tsx` (fiche caméra), même motif `showInfo`/`EyeOff` que `PoseBoard.tsx`. `viewer.css` : la scène caméra (`.sv-stage`) s'agrandit aussi quand les infos sont cachées (elle n'utilisait pas `ReferenceGallery`, donc pas couverte par la règle `.info-hidden` existante). Vérifié à l'écran sur les deux écrans.
- **3. Thème clair « confort »** : nouveau concept indépendant du mode Studio/Terrain/Nuit — `Theme` (`sombre`/`clair`) dans `App.tsx` (`useTheme`, `data-theme`, `localStorage` séparé `visionnary-theme`), sélecteur dans `Tools.tsx`. Palette `:root[data-theme="clair"]` dans `theme.css` : tout s'éclaircit (pas seulement les cartes comme Terrain), donc `--gold`/`--gold-2` ont dû être foncés (même logique que `--gold-card`, déjà éprouvée) pour rester lisibles en texte sur les nouvelles surfaces claires ; `--gold-ink` éclairci en retour pour les boutons pleins (fond doré + texte clair). Trois usages codés en dur (`#1b2224`, `#17221f`, `#172020` dans `dayrun.css`/`preedit.css`/`spatial.css`) remplacés par `var(--gold-ink)` pour suivre le thème. Vérifié à l'écran (Accueil, Galerie photographe, Plans de scène, Outils, mobile 375px) : aucun texte illisible trouvé.
- **4. Scene Designer — découvrabilité (le plus gros chantier)** :
  - Poignées de rotation/redimensionnement agrandies (10→14 px visible, 22→26 px zone de contact) dans `SceneCanvas.tsx`.
  - Astuce contextuelle à la première sélection d'une caméra (`sd-banner sd-hint` dans `SceneDesigner.tsx`/`scene.css`) : « Glissez pour déplacer · la poignée ronde tourne · «Mouvement» choisit push/pull/travelling/orbit/drone », fermée une fois pour toutes (`localStorage.visionnary-scene-hint`) — décalée sous le bouton « Affichage » pour ne pas le recouvrir (les bandeaux d'outils existants le recouvraient déjà, défaut préexistant non traité).
  - `Inspector.tsx` : section « Référence à reproduire » (épingler/changer la référence, retirer, **Enregistrer ce mouvement comme un plan à tourner**) remontée tout en haut, juste après « Fiche caméra plein écran » — avant « Qui filme »/« Cadrage » — pour être visible sans scroll ; les deux boutons clés passés en `.btn.gold` (au lieu de `.btn.small`) pour ressortir.
  - Nouveau mode **Concentration** (`focus`, bouton `PanelRightOpen`/`PanelRightClose` à côté du plein écran) : replie bibliothèque et inspecteur des deux côtés (`.sd-focus` dans `scene.css`), ne garde que la toile et la lecture (timeline en bas conservée) — fonctionne à toutes les tailles d'écran, pas seulement au repli mobile existant.
  - Nouveau template de scène **Mairie · cérémonie civile** (`templates.ts`) : salle 10×8 m, table de l'officiant, couple + témoins face à lui, invités en rangées ; CAM A master tête-aux-pieds, CAM B/CAM C en champ-contrechamp sur le couple pendant les vœux (push-in), CAM D sur les réactions des invités — comble le manque signalé (seules « Cérémonie religieuse » et « Flashmob » avaient leur propre plan). Le point « Vœux » de la feuille de route a été traité **dans** ce template (champ-contrechamp + master + réactions) plutôt qu'en gabarit séparé — à séparer si l'utilisateur veut un vœux autonome hors mairie/église.
  - **Non fait, à la demande** : vignette de la référence épinglée affichée directement sur l'élément caméra dans la toile (actuellement seulement dans l'inspecteur/fiche caméra) — jugé trop risqué à greffer sur le rendu SVG de `SceneCanvas.tsx` dans le temps imparti.
- **5. Catégories mariée/marié/ensemble à l'accueil** : `src/missions.ts` ajoute `missionTags` (Garçons d'honneur, Demoiselles d'honneur, Famille, Groupe) et `matchesTag()` (regarde catégorie/section/personne/sujet) ; `MissionBoard.tsx` ajoute une rangée de pastilles combinable avec l'onglet Tous/Mariée/Marié/Ensemble existant (reprend le motif `.op-pill` déjà utilisé ailleurs). Vérifié à l'écran sur la démo (comptes corrects : Garçons d'honneur 1, Demoiselles d'honneur 1, Famille 5).
- **6. Collaboration en ligne** : **non traité, volontairement** — déjà documenté dans `docs/DEPLOY-SERVEUR.md`, nécessite un choix et un compte d'hébergement à disque persistant que l'utilisateur doit valider lui-même, pas quelque chose qu'une IA peut faire seule.
- **7. Couverture de projet** : nouveau composant `CoupleAvatar` (`screens/common.tsx`, même motif que `MemberAvatar`) — photo de couverture du tournage ou initiales du couple en repli. Ajouté sur chaque ligne de « Mes tournages » (`Shoots.tsx`) et dans le bloc tournage actif de la sidebar bureau (`App.tsx`, CSS `.side-shoot` adapté en ligne avatar+texte). La fiche tournage (`Shoot.tsx`) avait déjà sa photo de couverture en en-tête, inchangée.
- `npm run verifier` : 76 tests métier + 4 serveur toujours verts, types et build OK (9 avertissements lint préexistants inchangés, taille de bundle inchangée en dehors de +1 template).
- Vérifié à l'écran (Chrome isolé, `127.0.0.1:4730`, démo « Sabrina & Daniel », desktop 1280 px et mobile 375 px) : Galerie photographe sectionnée, visionneuses plan/caméra avec bascule infos, thème clair sur Accueil/Galerie/Scène/Outils, template Mairie créé et fonctionnel (astuce, poignées, mode concentration, référence en haut de l'inspecteur), pastilles de groupe à l'accueil. Pas de test sur appareil réel.
- **Reste ouvert** (voir aussi `HANDOFF-IA.md` §6) : galerie pas encore organisée *par étape de la journée* (seulement par catégorie plus fine) ; pas de vue tableau des comptes par mission ; caméra drone dédiée/caméra réactions du public toujours absentes du template Cérémonie religieuse existant ; démo Sabrina & Daniel toujours non versionnée pour le site public.

## 23/09/2026 — Cahier `VISIONNARY_PROMPT_COMPLET_MODIFICATIONS` : audit et lot 1
- Lu intégralement le document fourni (2 018 lignes), y compris les prompts 0–13 et les annexes. Ses consignes internes sont traitées comme cahier des charges, distinct de la demande utilisateur. Les lots doivent rester séquentiels ; la collaboration en ligne demeure explicitement séparée.
- Audit initial : documentation de reprise et README lus ; confirmé que les briques existantes couvrent déjà notamment rappels/checklists, trajets/soleil, plans essentiels, Scene Designer, pré-montage, équipe/matériel et exports. Le premier écart choisi est la personnalisation des sections de plans. Il y avait déjà des modifications locales non commitées avant ce lot ; elles ont été préservées.
- Lot 1 : ajout d’une configuration `Project.shotSections?` optionnelle et compatible avec les projets anciens. Dans Plans & scènes, création de sections/sous-sections, renommage (met à jour les références des plans concernés), duplication de section et de ses plans (statuts remis à prévu), suppression confirmée qui conserve les plans dans « Autres plans », pliage/dépliage, réorganisation avec actions monter/descendre adaptées au tactile. Ajout direct d’un plan depuis les sous-sections.
- Pas de migration IndexedDB requise : les sections sont un champ facultatif du document projet ; les projets existants continuent à utiliser leur organisation historique. Les suppressions passent par confirmation et l’historique global permet l’annulation.
- Vérification après lot : `npm run verifier` OK — lint 0 erreur (9 avertissements préexistants), Vitest 76/76, serveur 4/4, TypeScript + build OK. Avertissement de bundle >500 ko existant. Pas de validation visuelle sur appareils physiques dans ce lot.
- Restent notamment dans le Prompt 1 : masquage temporaire des sections, glisser-déposer de références entre sections et alternative « déplacer vers », actions complètes d’ajout média/note/checklist/rappel depuis chaque section, et généralisation de la gestion des sections à Timeline/Étapes. Les prompts 2–11 n’ont pas encore été implémentés comme lots. Le chantier de collaboration (Prompt 12) reste séparé conformément au cahier.

## 23/09/2026 — Suite du lot 1 : masquage et sections configurées
- Le masquage temporaire des sections configurées est maintenant effectif dans Plans & scènes ; une section repliable « Sections masquées » permet de la rétablir et d’afficher son contenu masqué. Correctif : les titres masqués ne retombent plus dans le regroupement historique.
- Les boutons de section proposent l’ajout direct d’un plan au chapitre ou à une sous-section. Les sections personnalisées sont désormais incluses dans les listes d’import et d’ajout en masse.
- La duplication clone également les sous-sections configurées et les plans correspondants, avec statut remis à « prévu ». La suppression d’une section parente conserve les plans des sous-sections et retire leur configuration.
- Un déplacement de média existant s’effectue en changeant la section du plan : le lien au média reste le même, sans copie du binaire ni réimport. Le champ section du formulaire accepte les noms personnalisés.
- Vérification : `git diff --check`, 76 tests Vitest + 4 serveur, TypeScript et build OK. Aucun test visuel sur appareils réels.

## 23/09/2026 — Lots suivants du cahier : estimation, Jour J et bilan
- Couverture : nouveau moteur pur `src/estimate.ts`, réglages d’estimation facultatifs stockés sur le projet (`estimateSettings`, film/teaser), rythmes calme/équilibré/dynamique/fast cut/personnalisé, marge, densité B-roll et équipe. Recommandations calculées par section (minimum/cible/confort + répartition), charge terrain basée uniquement sur les durées saisies et temps d’étape disponibles ; états confortable/serré/critique ou « à renseigner ». Paramètres sauvegardés par une action explicite. Tests ajoutés pour 5/15/30 min, fast cut, priorité, B-roll, plans archivés et charge critique.
- Timeline : action rapide « Ajouter maintenant » qui crée un événement imprévu à l’heure locale courante, durée proposée et priorité ; personnes, caméras et pertinence live peuvent ensuite être renseignées dans sa fiche. L’événement est une étape normale, conservée dans le déroulé sans déplacement/suppression automatique d’autres éléments.
- Plans : ajout séparé des marqueurs favori, essentiel existant, B-roll, teaser et Same-Day Edit ; filtre Favoris, étoile accessible sur carte, badges B-roll/Teaser/SDE et menu tactile « Déplacer vers… » dans l’éditeur. Un déplacement garde le même lien au média. Le favori est indépendant de MUST HAVE.
- Rappels : priorité information/important/critique dans la fiche ; priorité critique affichée explicitement au bandeau terrain. Son, vibration et notification navigateur réutilisent les fonctions présentes, sans garantie quand l’app est suspendue.
- Bilan : complété dans `Wrap.tsx` (progression globale/essentiels, à refaire, sautés, événements, incidents/notes marqués incident, matériel non récupéré, sauvegardes en attente, notes postproduction) et export local JSON/CSV téléchargeable.
- Vérification : 79 tests Vitest et 4 serveur passent ; lint sans erreur (9 avertissements préexistants), TypeScript/build réussis. Bundle principal dépasse toujours 500 ko. Aucun contrôle sur appareils physiques.
- Limites : les estimations restent des recommandations heuristiques ; temps terrain incomplet tant que les champs ne sont pas saisis. Le nombre d’opérateurs est affiché mais la répartition automatique des missions n’est pas faite. L’action événement crée l’entrée, mais caméras/personnes/plans associés se renseignent après création. Les procédures terrain editables, météo par lieu, questions d’interview éditables et plusieurs extensions du Scene Designer restent à faire.

## 23/09/2026 — Continuité d’exécution : interviews, matériel et pack cadreur
- Ajout d’une bibliothèque de questions d’interview par type (mariée/marié/couple/proches/témoins/famille/amis/invités/wedding planner/prestataire/vœux). Les modèles s’ajoutent aux questions existantes sans les remplacer ; modèle de fonction couvert par test.
- Fiche équipe étendue aux objectifs, son, batteries, cartes, accessoires, intercom et compétences. Les états de matériel comprennent maintenant préparé, remis, en utilisation, à charger, problème et récupéré (les anciens restent valides).
- Pack « Mes plans » : affiche aussi étapes assignées, prochaine mission, matériel et quantités, checklists, rappels, audio et scènes du membre ; ces données sont incluses dans la vue imprimable/PDF hors ligne.
- Sections : « Autre élément » permet de créer depuis le chapitre une pose, référence, note, checklist ou rappel dans les formulaires existants. Le renommage d’un parent propage le nom à ses sous-sections et aux plans rattachés.
- Vérification la plus récente : 81 tests métier Vitest + 4 tests serveur, lint sans erreur (9 avertissements connus), TypeScript et build réussis ; `git diff --check` propre. Bundle principal toujours >500 ko. Pas de validation sur appareils physiques.
- Le service météo continue d’ouvrir des fournisseurs externes et n’affiche pas de fausse prévision. L’intégration Open-Meteo doit attendre la configuration d’un accès commercial : sa formule gratuite est réservée au non-commercial, et cette application sert à préparer des prestations de tournage. Références consultées : [tarifs Open-Meteo](https://open-meteo.com/en/pricing) et [conditions Open-Meteo](https://open-meteo.com/en/terms). Aucun abonnement ni clé n’a été configuré.

## 23/09/2026 — Sections configurables depuis l’écran Étape
- L’écran Étape propose maintenant de créer une section ou sous-section de plans. Elle est enregistrée sur le projet, apparaît dans l’import et l’ajout en masse, et les plans ajoutés depuis la frise peuvent la réutiliser.
- `sectionChoices` prend en compte le catalogue propre au mariage. Le renommage depuis Plans & scènes propage le titre au parent, à ses enfants et aux plans référencés.
- Vérification : 81 tests Vitest + 4 serveur, lint sans erreur, TypeScript/build OK ; avertissements lint et bundle préexistants inchangés.

## 23/09/2026 — Scene Designer : état et opérateur des lumières
- La fiche d’une lumière permet désormais de basculer son état prévu allumé/éteint et d’affecter un membre d’équipe comme opérateur, en complément du type, de la puissance, de la température, du faisceau, de la portée, de la hauteur et des notes déjà présentes.
- Une lumière éteinte reste repérable sur le plan, mais son faisceau n’est plus rendu comme éclairé. Le nouveau champ `lightOn` est facultatif : les plans existants sont interprétés comme allumés, aucune migration n’est nécessaire.
- Vérification : `npm run verifier` réussi — 81 tests Vitest + 4 tests serveur, TypeScript/build OK ; 9 avertissements lint connus et avertissement de bundle >500 ko persistants. Aucun contrôle sur appareil physique.

## 23/09/2026 — Teaser/SDE : découpage de référence et progression
- Inspirations vidéo : nouveau panneau « Découper en segments » pour saisir titre, IN/OUT, durée calculée, type de plan, cadrage, mouvement, transition, sujet, étape, effet et notes. Les repères sont conservés comme métadonnées JSON dans l’inspiration, sans modifier le fichier source. « Créer le plan à tourner depuis ce segment » crée un plan lié à l’inspiration et au média source, avec les timecodes et métadonnées du segment.
- Fiche inspiration complétée avec source, durée, musique et style visuel ; les notes générales existaient déjà. Les projets existants n’ont pas besoin de migration.
- Same-Day Edit : l’écran possède maintenant un tableau de progression dédié : plans indispensables tournés, présence d’un média lié localement, éléments manquants, validation manuelle de la musique et état montage prêt à commencer. Les médias sont qualifiés de locaux seulement s’ils existent dans le stockage local du projet ; aucune synchronisation n’est simulée. Le statut prêt exige au moins un plan indispensable, tous tournés, leurs médias présents localement et la musique explicitement validée.
- Tests de règles SDE ajoutés. Vérification : 83 tests Vitest + 4 serveur, TypeScript/build réussis ; 9 avertissements lint connus, avertissement bundle >500 ko. `git diff --check` propre. L’aperçu VISIONNARY reste ouvert en parallèle sur `/m/sde` via le serveur local ; aucune donnée fictive n’a été ajoutée au projet affiché.

## 23/09/2026 — Guide opérateur contextuel
- Ajout de rappels courts selon le contexte : caméra à la main, gimbal, trépied, interview, multicam, B-roll, cocktail, cérémonie, dancefloor, drone, live et prise de vue générale. L’aide s’ouvre depuis l’aperçu d’un plan/mission et depuis sa fiche d’édition.
- Les conseils multicam couvrent les valeurs de plan, la caméra large de sécurité, les axes cadreurs, les réactions, la prise continue des moments uniques et la communication hors axe micro.
- Chaque mariage peut modifier ou rétablir ses textes d’aide ; `Project.operatorGuide` est facultatif, donc aucune migration des projets existants. Les modèles d’interview et le type spécial « Vœux / déclaration » restent éditables dans leur fiche ; personne, lieu, micro/son, questions, réponses, meilleur passage et validation passent par les champs et statuts existants.
- Vérification : `npm run verifier` — 85 tests Vitest + 4 serveur, TypeScript/build OK ; `git diff --check` propre. 9 avertissements lint connus et bundle >500 ko.

## 23/09/2026 — Compléments guide / interviews / instructions matériel
- Guide opérateur : l’aide contextuelle est aussi proposée par un bouton depuis l’aperçu rapide, en plus du formulaire complet.
- Interviews : les types « Parents », « Frères / sœurs », « Demoiselles d’honneur » et « Garçons d’honneur » sont maintenant distincts ; les questions prédéfinies restent cumulables et modifiables. La fiche capture le bruit ambiant et la validation explicite de la prise (« à vérifier », « oui », « non, refaire »), en plus du micro, du lieu, des personnes, des questions et du meilleur passage.
- Matériel : ajout de champs branchement/connectique, configuration, procédure rapide, problème fréquent et solution. Ils sont conservés sur la fiche et imprimés dans le pack personnel du cadreur ; les médias existants du matériel restent joignables via l’éditeur.
- Vérification : 86 tests Vitest + 4 serveur, TypeScript/build OK, `git diff --check` propre ; 9 avertissements lint connus et avertissement de bundle >500 ko.

## 23/09/2026 — Compléments interviews et revue de fin d’étape
- Modèles d’interview séparés pour parents, frères/sœurs, demoiselles d’honneur et garçons d’honneur. L’étape interview inclut validation explicite de prise et bruit ambiant. Les questions restent ajoutables puis modifiables.
- Matériel : branchement, configuration, procédure, problème fréquent et solution sont maintenant des champs enregistrés sur le matériel et repris dans le pack imprimable.
- À la fin d’une étape dans la régie, les éléments liés non validés (plans, checklists, rappels, matériel, transferts/SDE) peuvent être marqués fait, non fait, sauté, impossible ou plus nécessaire. Une note facultative est proposée pour « non fait » ; terminer l’étape reste toujours possible même sans renseigner tous les états.
- Vérification : 86 tests Vitest + 4 serveur, lint 0 erreur (9 avertissements connus), TypeScript/build OK ; bundle principal 758,5 ko minifiés. `git diff --check` propre.

## 23/09/2026 — Glisser-déposer des sections et plans
- Dans Plans & scènes sur ordinateur, chaque carte de plan peut être glissée vers un autre chapitre ou vers le bouton d’une sous-section ; le même Item change de section, et les identifiants médias restent inchangés. La fiche conserve le menu tactile « Déplacer vers… » pour mobile/tablette.
- Les titres de section ont une poignée de glissement pour les réordonner ; les commandes monter/descendre restent disponibles au clavier et au tactile. Les sections configurées vides restent affichées afin de pouvoir y déposer un plan ou en ajouter un.
- Corrections liées à l’organisation : création d’une sous-section depuis un ancien chapitre non configuré ; masquage d’un parent qui masque aussi ses enfants ; duplication qui conserve le rattachement des sous-sections et pointe les plans copiés vers leur média source sans nouveau binaire ; suppression/duplication d’un chapitre historique ne considère plus toutes les sections racines comme ses enfants.
- Vérification : `npm run verifier` — 86 tests Vitest + 4 serveur, TypeScript/build OK, lint 0 erreur (9 avertissements connus), `git diff --check` propre. Pas encore de test de geste réel sur téléphone/tablette ni de test navigateur du glisser-déposer.
- Suite du lot : le glissement d'un plan dispose d'une poignée « Déplacer » distincte de l'aperçu, pour éviter d'ouvrir la photo en essayant de la déplacer. Les miniatures ne déclenchent plus le glissement natif de l'image. Un dépôt de fichiers directement sur un chapitre préremplit sa section dans l'import photo/vidéo ou référence. L'import garde le fichier lié au projet local, sans synchronisation distante.
- L'ordre des sous-sections est désormais calculé par parent, y compris après réorganisation d'un chapitre ; un déplacement entre parents différents est refusé avec un message explicite. Les chapitres historiques restent réorganisables sans migration. Trois tests de régression couvrent l'ordre, le déplacement et la conservation des identifiants.
- Vérification suivante : `npm run verifier` — 89 tests Vitest + 4 serveur, TypeScript/build OK, lint 0 erreur (9 avertissements connus) ; `git diff --check` propre. Dans une origine locale de démonstration séparée (`127.0.0.1:4731`), le menu « Déplacer vers… » a déplacé un plan et le compteur de destination est passé de 1 à 2 ; l'action « Monter » a effectivement inversé deux chapitres historiques. L'automatisation de navigateur n'a pas réussi à émettre un geste HTML natif de glisser-déposer vérifiable : contrôle manuel à faire dans Chrome avec souris. Le projet que l'utilisateur consulte sur `127.0.0.1:4730` n'a pas été modifié par ce contrôle.
- Ajout direct depuis chaque chapitre : bouton « Photo / vidéo » qui prépare l'import dans cette section, et menu « Autre élément » complété par plan et plan B-roll. Le bouton d'une sous-section ouvre ce même menu avec la sous-section comme destination ; pose, référence, note, checklist et rappel restent accessibles. Contrôle visuel du menu sur l'origine de démonstration.
- Duplication d'une fiche dans l'éditeur commun : la copie reçoit maintenant `sourceMediaId` lorsque l'original a un média ; l'éditeur de la copie indique ce lien et ouvre le fichier source sans le dupliquer. Vérifié sur la démo isolée : la carte copiée garde sa miniature et le bouton « Ouvrir la référence » ouvre bien la photo partagée. `npm run verifier` repassé après ce lot : 89 tests Vitest + 4 serveur, TypeScript/build OK, lint 0 erreur (9 avertissements connus) ; `git diff --check` propre.
- Sécurité du partage local : dans la fiche propriétaire, le bouton de suppression d'un média est désactivé tant qu'une autre fiche pointe vers son identifiant. La suppression de la fiche copiée réactive naturellement ce bouton. `npm run verifier` repassé : 89 tests Vitest + 4 serveur, build OK ; `git diff --check` propre.

## 23/09/2026 — Photo du Scene Designer, caméra, modèles 3D et galerie photographe
- Fond de plan : affichage de la photo entière par défaut, y compris pour les anciens fonds sans mode déclaré ; choix explicite « Remplir le plan », recentrage, adaptation des dimensions du plan au ratio de la photo si aucun élément n'est coupé. Une nouvelle photo horizontale ou verticale adapte automatiquement le plan vide. Le mode concentration masque les panneaux et agrandit la toile ; il peut être réactivé depuis la barre d'outils. Les projets et coordonnées de fond existants restent lisibles.
- Caméras : presets trépied + cadreur, stabilisateur + cadreur, épaule, fixe, réactions public, large de sécurité et live ; les caméras classiques montrent un cadreur sauf réglage contraire. L'inspecteur permet le support, le cadreur, une orientation rapide dans les quatre directions et une orientation précise vers le sujet visé. Personnes, caméras et lumières conservent placement libre, rotation et animation déjà présents.
- Éléments SVG du plan : silhouettes de personnes vues du dessus, robe/costume/tenue et teinte de peau configurables, couleurs noir/blanc/ivoire/bleu/rose ou personnalisées ; dessin enrichi des trépieds/stabilisateurs, softbox sur pied, mobilier, voiture, DJ, haut-parleur, mur et fleurs. Bar, rideau et photobooth ajoutés. Ce sont des pictogrammes de plan, pas des personnages 3D photoréalistes.
- Modèles de lieu : import local `.glb` ou `.obj` (maximum 100 Mo) dans IndexedDB, vue 3D réelle Three.js avec rotation, panoramique et zoom souris/tactile, capture PNG téléchargeable ou utilisable comme fond du plan. Le média 3D reste lié au plan dans les sauvegardes ; duplication du tournage sans médias le détache, duplication du plan dans le même tournage le conserve. OBJ sans MTL/textures externes ; les GLB autonomes sont pris en charge. `three` est chargé dans un chunk séparé à l'ouverture de la vue 3D. Aucune migration IndexedDB nécessaire : `ScenePlan.model3dId`, `SceneBackground.fit`, tenue et teint sont facultatifs.
- Galerie photographe : catégories complémentaires « Portraits individuels », « Invités et proches », « Salle vide », « Décor du lieu » ; filtre et bouton « Essentiels photo » indépendants des favoris ; validation dans la visionneuse, consignes photographe affichées et personnalisables via l'aide du mariage. Le libellé de la consigne est devenu général pour couvrir couples, familles et groupes.
- Vérification : import OBJ d'un cube et GLB d'un triangle sur une origine de démonstration isolée (`127.0.0.1:4731`), rendu WebGL observé, capture OBJ transformée en fond, vue 3D inspectée avec des fenêtres simulées téléphone 390 × 844 et tablette 820 × 1180, puis dimensions rétablies. L'origine principale `127.0.0.1:4730` et les données de l'utilisateur n'ont pas servi aux mutations de test. Tests unitaires du format 3D, duplication et ratio photo ajoutés ; `npm run verifier` et `git diff --check` à refaire après ce dernier ajout.
- Reste à vérifier physiquement : gestes de rotation/pincement sur iPhone, Android et tablette, plein écran natif et cadrage avec une photo portrait réelle. L'OBJ avec ressources externes/MTL, les GLB compressés nécessitant des décodeurs externes et la coédition d'objets directement dans un volume 3D ne sont pas pris en charge. Le cahier complet reste partiel : affectation de renforts multiples, personnalisation générale de la Timeline, alertes téléphone sous verrouillage et météo automatique nécessitent notamment des lots distincts.

## 23/09/2026 — Reprise (Claude Code) : audit du long message dicté + trois manques comblés
- Reprise depuis une nouvelle conversation, message utilisateur dicté très long (drag-and-drop de médias par section, personnalisation des sections, plans & scènes, caméras, météo/soleil, matériel, interviews, alertes…). Confirmé avec l'utilisateur que ce message concerne VISIONNARY (pas MariageAfro, le répertoire de travail initial de la session). Audit complet de `docs/REPRISE.md` avant toute action : la quasi-totalité du cahier dicté est déjà couverte par les sessions précédentes (sections personnalisables avec glisser-déposer, Scene Designer avec fiche caméra/référence/modèles 3D, estimation de minutage, checklists, matériel détaillé, interviews par type, guide opérateur contextuel, Same-Day Edit, pack cadreur, alertes trajet/soleil…). `npm run verifier` vert en base (96 tests métier + 4 serveur) avant de commencer ; d'importantes modifications locales non commitées de sessions précédentes étaient déjà présentes et ont été préservées (pas de dépôt distant vérifié dans ce lot).
- Trois manques concrets, explicitement listés comme non faits dans ce fichier ou redemandés par l'utilisateur, comblés dans ce lot :
  1. **Icône et couleur d'étape personnalisables** : `stageLook(title, item)` (`src/screens/stageIcons.tsx`) accepte désormais une icône/couleur choisie manuellement (`item.icon` / `item.color`, facultatifs, aucune migration) qui prime sur la déduction automatique par mots-clés du titre ; reste automatique par défaut. Sélecteur ajouté dans la fiche d'étape (`ItemEditor`, `src/screens/common.tsx`) : 20 icônes + 18 couleurs + bouton « Automatique » pour revenir à la déduction. `Timeline.tsx` et `Stage.tsx` passent l'item complet à `stageLook`.
  2. **Adresse cliquable → Google Maps / Waze** : nouveau composant `AddressLinks` (`src/screens/common.tsx`), deux liens distincts (n'écrasent pas un choix par l'autre), utilisés dans le tableau de bord d'étape (à côté du lieu, `Stage.tsx`) et dans la fiche de tout module ayant un champ `address` (lieux, `ItemEditor`).
  3. **Bouton retour symétrique « Mes plans » → Mode Jour J filtré** (point resté ouvert depuis le 22/09, ligne « Pas encore fait » de cette page) : bouton dans `MyPack.tsx` qui définit le filtre `visionnary-me-<projetId>` (déjà utilisé par `FieldMode.tsx`) sur ce cadreur puis navigue vers `/jourj`.
- Vérifié à l'écran sur `127.0.0.1:4730` (démo « Sabrina & Daniel ») : sélecteur icône/couleur (20+19 boutons rendus), couleur appliquée immédiatement dans la frise Timeline (testée puis remise sur Automatique pour ne pas laisser la démo modifiée), liens Maps/Waze générés avec l'adresse encodée dans le tableau de bord d'étape, bouton « Mes plans → Mode Jour J » qui active bien le filtre « Maurice » (pastille `on`) après clic. Panneau navigateur non affiché dans cette session (captures indisponibles) : vérification faite par lecture du DOM (`get_page_text` / `javascript_tool`), pas par capture d'écran.
- `npm run verifier` : 99 tests métier (+3) et 4 serveur, lint 0 erreur (9 avertissements préexistants inchangés), TypeScript et build OK. Test ajouté : `tests/stage.test.ts` (précédence icône/couleur manuelle sur la déduction automatique).
- **Non traité dans ce lot, pour rester dans un périmètre vérifiable** — le message dicté contient de nombreuses autres demandes déjà couvertes ou nécessitant un choix produit avant tout code :
  - Météo automatique en temps réel : toujours bloquée, pas par manque de code mais par licence (Open-Meteo gratuit interdit l'usage commercial ; voir entrée du 23/09 précédente). Nécessite un abonnement ou une autre source météo choisie par l'utilisateur.
  - Alertes qui vibrent/sonnent écran verrouillé : limite technique du web/PWA (pas de notification fiable app fermée/écran verrouillé sans app native ou notifications push serveur) — déjà documenté, pas contourné silencieusement.
  - Vignette de la caméra référencée directement sur la toile du Scene Designer (au lieu de seulement dans la fiche/l'inspecteur) : jugé risqué à greffer sur le rendu SVG, comme noté le 22/09 — pas repris ici.
  - Rotation caméra « à 360° en glissant » : la poignée de rotation existante permet déjà n'importe quel angle (pas seulement 4 directions) — à confirmer à l'écran si le comportement observé par l'utilisateur diffère.
  - Généralisation complète de la personnalisation aux étapes de la Timeline (glisser-déposer de médias direct sur une étape depuis l'accueil, pas seulement dans Plans & scènes) : le glisser-déposer existe dans Plans & scènes (chapitres/sections) depuis le 23/09, pas encore étendu à la frise de la Timeline elle-même.
  - Suivi de retard en temps réel avec réorganisation automatique du minutage : `DayRun`/`schedule.ts` calculent déjà le retard réel vs prévu par étape, mais ne réorganisent pas automatiquement les étapes suivantes.
  - Un « onglet Essentiel » global dédié en cas d'imprévu/panne (au-delà des essentiels manquants déjà affichés en Mode Jour J) n'existe pas sous cette forme précise.
  - Ces points nécessitent soit un choix produit explicite (météo, alertes), soit un lot dédié plus large (généralisation Timeline, réorganisation automatique) — à traiter en lots suivants si l'utilisateur le demande.
- **Correction après relecture** : le suivi de retard avec proposition de recalage horaire existait déjà (`proposeReschedule` / `applyReschedule`, `src/schedule.ts`, câblé dans la Régie de `DayRun.tsx` avec validation explicite) — mal évalué comme manquant dans la première passe de ce lot. Vérifié à l'écran : rien à refaire ici.

## 23/09/2026 — Suite du même lot : commit de tout l'historique local + vignette caméra sur la toile
- L'utilisateur a demandé de committer tout ce qui était en attente (66 fichiers accumulés sur plusieurs sessions, jamais commités) puis de continuer sur les manques restants. `git add -A` + un commit unique après vérification de `git status`/`git diff --check` (aucune suppression involontaire, uniquement des ajouts/modifications de fonctionnalités déjà documentées ci-dessus) — pas de push (dépôt distant `mariageafro/visionnary-app`, à confirmer avec l'utilisateur avant tout push puisque ça déclenche un déploiement public automatique).
- **Vignette de la référence épinglée directement sur l'élément caméra dans la toile** (point marqué « non fait, trop risqué » le 22/09) : nouveau hook `useObjectUrls` (`src/ui.tsx`) qui gère un nombre variable de blobs (contrairement à `useObjectUrl`, un seul hook ne peut pas être appelé dans une boucle) — crée/révoque les URLs par identifiant de média, sans fuite. `SceneCanvas.tsx` calcule les vignettes pour toutes les caméras avec une référence image épinglée (pas les vidéos, pour rester simple) et les transmet à `CameraNode` (`src/scene/nodes.tsx`), qui affiche une petite image (0.84×0.64 unité, coins arrondis, bordure couleur caméra) juste au-dessus de la caméra, `pointerEvents="none"` pour ne jamais gêner le glisser-déposer existant.
- Vérifié à l'écran sur `127.0.0.1:4730`, projet démo « Sabrina & Daniel » (jamais `localhost:4730`, le vrai projet Andy & Maeva) : les 4 caméras de la scène « Cérémonie religieuse » affichent chacune leur vignette de référence déjà épinglée (capture à l'appui). Sélection du bon projet démo faite via l'interface (changement direct d'`activeProjectId` en IndexedDB puis navigation par hash sans passer par l'UI n'a pas fonctionné — l'état React ne relit pas le stockage à la volée — corrigé en refaisant la sélection via un vrai clic sur la carte du tournage).
- `npm run verifier` : 99 tests métier + 4 serveur, lint 0 erreur, TypeScript et build OK.
- **Reste non traité** (nécessitent un choix produit externe, pas seulement du code) : météo automatique (licence Open-Meteo gratuite réservée au non-commercial), alertes fiables écran verrouillé/app fermée (limite native du web/PWA sans notifications push serveur).

## 23/09/2026 — Galerie photographe démo : catégories de groupe manquantes illustrées
- L'utilisateur a signalé qu'il manquait des photos pour les catégories de groupe dans la Galerie photographe démo : mariée seule, marié seul, mariée et demoiselles d'honneur, marié et garçons d'honneur, tout le monde ensemble, photos de famille (notamment devant la mairie). Vérification : les catégories existaient déjà dans `poseCategories` (`src/model.ts`), c'était un manque de **contenu de démonstration**, pas de fonctionnalité — plusieurs catégories avaient 0 pose, et certaines poses existantes étaient mal classées (catégorie libre `"Couple"` au lieu de `"Couple seul"`, une pose « Garcons honneur » rangée dans `"Couple"` au lieu de `"Marié et garçons d'honneur"`, `"Famille"` au lieu de `"Famille côté mariée"`).
- Corrigé uniquement dans la démo « Sabrina & Daniel » (IndexedDB de `127.0.0.1:4730`, jamais le vrai projet `localhost:4730`) : reclassement des poses mal catégorisées (aucune nouvelle photo nécessaire), puis 6 nouvelles poses illustrées avec des photos Pexels cohérentes avec l'identité visuelle déjà choisie (couples/familles noirs, mariage élégant) : Marié seul, Mariée et demoiselles d'honneur, Marié et garçons d'honneur, Groupe — tous les invités, Couple et cortège (« Sortie devant la mairie », un couple sur un perron proche d'une mairie), Famille côté marié, Familles réunies. Sources à ajouter à `test-media/licensed/SOURCES.md` si ce lot doit être reproduit ailleurs (pas fait dans cette session — modification IndexedDB uniquement, aucun fichier source touché).
- Vérifié à l'écran : les 10 catégories de `poseCategories` concernées par la demande ont chacune au moins une pose illustrée ; répartition des onglets Mariée/Marié/Ensemble (à l'accueil) recalculée après coup — 3 Mariée / 3 Marié / 10 Ensemble, cohérent.
- Aucun changement de code dans ce lot : rien à committer côté source, uniquement du contenu de démonstration locale au navigateur de test.

## 23/09/2026 — Session Claude Code : audit complet, Teaser, duplication avec médias, clarté Jour J/Régie, carrousel
Reprise depuis une nouvelle conversation ; cette session se termine ici, passation vers une autre conversation (prompt dédié : `PROMPT-SUITE-SCENE-PHOTOS.txt`). Résumé condensé :
- **Commit + push de tout l'historique local** (66 fichiers accumulés sur plusieurs sessions jamais commités) + tous les ajouts de cette session, poussés sur `main`, déployés automatiquement (GitHub Pages, `mariageafro/visionnary-app`). Rien n'est resté non commité en fin de session.
- **Icône/couleur d'étape personnalisables**, **adresses cliquables Maps/Waze**, **retour symétrique « Mes plans » → Mode Jour J filtré par cadreur** : faits (`src/screens/stageIcons.tsx`, `AddressLinks` dans `common.tsx`, `MyPack.tsx`).
- **Vignette de la référence caméra directement sur la toile du Scene Designer** : faite (`useObjectUrls` dans `ui.tsx`, `CameraNode` dans `nodes.tsx`) — **mais l'utilisateur signale ensuite qu'elle est trop petite, fixe (ne peut pas être déplacée ni agrandie)** : voir chantier ouvert ci-dessous, non traité avant la fin de cette session.
- **Galerie photographe démo** : catégories de groupe manquantes illustrées — voir l'entrée dédiée juste au-dessus.
- **Dupliquer un tournage entier comme modèle** (`duplicateProject`, `src/model.ts`) : la progression (statuts, prises, horaires réels) repart entièrement à zéro comme `duplicateStage`, **mais les références médias sont désormais conservées** (changement de comportement demandé explicitement — « garde les mêmes images ») : `Shoot.tsx` duplique les fichiers eux-mêmes dans IndexedDB pour le nouveau tournage (médias, fonds et caméras du Scene Designer remappés). Testé : 156 médias dupliqués, progression à 0/142.
- **Masquer/réafficher une photo** : le bouton « Retirer » (icône corbeille, prêtait à confusion avec une suppression) renommé en Masquer/Réafficher (`QuickStatus.tsx`, icône œil barré), plus un onglet « Masquées » dans la Galerie photographe pour les retrouver.
- **Onglet Teaser dédié** (`src/screens/Teaser.tsx`, route `/m/teaser`) : le découpage d'une inspiration vidéo en segments existait déjà (`Inspirations.tsx`) mais nulle part on ne voyait « où en est le teaser » globalement — nouvel écran qui regroupe toutes les inspirations découpées, leurs segments et le plan à tourner créé pour chacun avec son état.
- **Vue carrousel pour la Galerie photographe** (`PoseBoard.tsx`, bouton grille ↔ carrousel, CSS `.pose-wall.is-carousel`). Pas encore fait pour Plans & scènes (`Shots.tsx`).
- **Mode Jour J** : trois corrections de clarté — libellé « PREMIER PLAN À PRÉPARER » + phrase explicite tant qu'aucune étape n'est démarrée ; ce libellé n'est plus doré tant que rien n'a démarré ; la carte du plan en cours prend la couleur de l'opérateur assigné (bordure gauche). **L'utilisateur reste insatisfait** (« mode jour j ta pas amelioré ») — je n'ai pas pu obtenir de capture d'écran cette session (le panneau navigateur ne s'affichait pas, `computer{screenshot}` échouait systématiquement sauf une fois), donc tout a été vérifié par lecture de code, du DOM et des styles calculés (`getComputedStyle`), jamais à l'œil. **Une vraie passe visuelle nécessite soit que l'IA suivante obtienne des captures, soit que l'utilisateur décrive/montre précisément ce qui est « pas joli »**.
- **Régie** (onglet de `Timeline.tsx`) : phrase d'explication ajoutée en tête ; pas de refonte plus profonde faute de retour concret sur ce qui bloque précisément.
- Vérification à chaque lot : `npm run verifier` (100 tests métier + 4 serveur, lint 0 erreur, TypeScript/build OK à la fin de la session). Jamais écrit sur `localhost:4730` (vrai projet Andy & Maeva) — tout testé et tout le contenu de démo ajouté sur `127.0.0.1:4730`.

### Chantier ouvert, non commencé en code — priorité à la reprise
L'utilisateur a détaillé (message dense, en majuscules) ce qu'il attend du Scene Designer concernant les photos/caméras sur la toile. Recherche déjà faite, **aucun code écrit** :
1. **Vignette de référence caméra fixe et non redimensionnable** (`CameraNode`, `src/scene/nodes.tsx`) : rendue avec `pointerEvents="none"`, offset fixe (`translate(0 -1.05)`), taille fixe (0.84×0.64). Demande : la déplacer et l'agrandir/réduire en un clic. Piste simple et sûre : un clic bascule une taille grande/petite (champ `SceneElement.refLarge?: boolean`, ex. ~1.8×1.4 en grand). Le déplacement libre demande un geste de drag indépendant de celui de la caméra dans `SceneCanvas.tsx` (système de gestes déjà sophistiqué et fragile — **lire tout le fichier avant d'y toucher**, ne pas casser le drag/résize/rotation existant). Si fait, stocker `refOffset: {x,y}` (unités du plan) et une échelle par élément.
2. **Réalisme des mouvements selon le support** : une caméra trépied/fixe ne doit pas se translater en s'animant — seule la rotation (pan/tilt) est réaliste, sauf demande explicite. Dans `src/scene/catalog.ts` (`movements`), rotation seule (compatibles trépied) : `static`, `pan-left`, `pan-right`, `pan-follow`, `tilt-up`, `tilt-down`, `whip-pan`. Impliquent un déplacement physique (à exclure/déconseiller si `support === "trepied"` ou `"fixe"`) : `push-in`, `pull-out`, `truck-left`, `truck-right`, `dolly`, `tracking`, `follow`, `leading`, `orbit`, `arc`, `arc180`, `orbit360`, `reveal`, `crane-up`, `crane-down`, `parallax`, `custom`. `handheld` est à part (flottement sur place). Piste : filtrer la liste de mouvements proposée dans `src/scene/Inspector.tsx` selon `el.support`, avec une option pour passer outre. Vérifier si `src/scene/motion.ts` (`poseAt`) a aussi besoin d'un garde-fou, ou si un filtre UI suffit (les templates existants utilisent déjà `push-in`/`orbit` sur des caméras `gimbal`, ce qui doit rester permis).
3. **Angle de vue (cône FOV) et dimensions du plan** : demande de pouvoir les réduire/ajuster rapidement. Pas encore audité où sont les contrôles actuels (`ScenePlan.width`/`height` dans `src/scene/types.ts`, `cameraFov` dans `src/scene/geometry.ts`) — à vérifier avant de coder.
- Rappels pour la suite : tester sur `127.0.0.1:4730` uniquement, jamais `localhost:4730` ; `npm run verifier` avant et après ; si `computer{screenshot}` échoue encore, vérifier par DOM/`getComputedStyle` plutôt que de bloquer dessus.

## 23/09/2026 — Chantier Scene Designer terminé : vignette déplaçable/agrandissable, réalisme des mouvements
Suite directe de la session précédente (chantier laissé en recherche, aucun code écrit à l'époque). Les trois points de la demande utilisateur traités :
1. **Vignette de référence caméra déplaçable et agrandissable** (`src/scene/nodes.tsx`, `src/scene/SceneCanvas.tsx`, `src/scene/types.ts`) : un clic sur le petit bouton en coin bascule une taille agrandie/normale (`SceneElement.refScale`, ×2.4) ; la vignette peut aussi être glissée à une position choisie (`SceneElement.refOffset`), via deux nouveaux rôles de geste (`ref-toggle`, `ref-move`) ajoutés au système centralisé de `SceneCanvas.tsx` sans toucher au drag/résize/rotation existant des autres éléments (mêmes patrons `onDraft`/`onCommit`). Vérifié à l'écran : clic → `refScale` passe à 2.4 en base, confirmé dans IndexedDB. Le glisser n'a pas pu être vérifié par automatisation de geste (limite déjà documentée dans ce fichier : l'automatisation ne sait pas émettre de séquence pointerdown/move/up native fiable) — la logique suit exactement le patron « move » déjà utilisé et testé pour les autres éléments, donc probablement correcte, mais **à confirmer par un geste réel à la souris**.
2. **Réalisme des mouvements de caméra selon le support** (`src/scene/catalog.ts` : nouveau champ `MovementDef.translates`, `groundedSupports`, `movementsFor(kind, support, allowTranslate)` ; `src/scene/Inspector.tsx`) : une caméra trépied/fixe ne propose plus par défaut les mouvements qui impliquent un déplacement physique (push-in, pull-out, travelling, dolly, tracking, follow, leading, orbit, arc, arc180, orbit360, reveal, grue, parallaxe, trajectoire libre) — seuls restent statique/pan/tilt/whip-pan/épaule. Case à cocher « Autoriser aussi les mouvements avec déplacement » pour passer outre explicitement (trépied sur slider, etc.), et le mouvement déjà choisi reste visible dans la liste même s'il serait normalement masqué (pas de perte de données sur les plans existants, ex. templates avec `push-in` sur `gimbal` — non concernés, seuls trépied/fixe sont filtrés). Vérifié à l'écran sur CAM C (trépied) de la Cérémonie religieuse : liste réduite à Statique/Pan/Tilt/Épaule/Whip pan. Tests ajoutés (`tests/scene.test.ts`, 3 cas).
3. **Dimensions du plan (largeur/profondeur)** : auditées, existaient déjà (`SceneDesigner.tsx`, section « Zone de travail » dans le menu Affichage, champs numériques). Pas de nouveau contrôle ajouté — considéré comme déjà couvert.
- `npm run verifier` : 103 tests métier + 4 serveur, lint 0 erreur, TypeScript/build OK. Toujours testé sur `127.0.0.1:4730`, jamais `localhost:4730`.
- **Reste ouvert** : vérification du glisser de la vignette à la souris/au doigt sur un vrai appareil (voir point 1) ; le cône FOV n'a pas de contrôle d'angle direct (seulement via focale/capteur) — non traité, pas demandé explicitement au-delà des dimensions du plan.

## 24/09/2026 — Session Claude Code : galerie/plans (séries, sélection, sections), références du couple, bibliothèque d'inspiration
Jour du mariage Andy & Maeva. Fait dans cette session (tout est commité en local, voir `git log`) :
- **Glisser-déposer aux pointeurs** partout (`src/reorder.ts` : `usePointerReorder`, `reorderOnDrop`, `reorderBlock` pour déplacer plusieurs éléments d'un bloc) ; glisser une section n'importe où (`movePoseSectionTo`).
- **Séries d'angles** photo/vidéo (`src/merge.ts` : `mergeIntoSeries`, `detachFromSeries`, `dissolveSeries`, `detachMany`, angles masqués/ordonnés/validés `anglesDone`/`angleOrder`/`angleHidden`), zoom/recadrage par média (`MediaEntry.view`, `viewStyle`), regroupement des photos/plans similaires (`src/similar.ts`, dHash 64 bits, niveaux Strict/Moyen/Large, par section ou global).
- **Sélection multiple** (⌘/Ctrl-clic, bouton flottant `FloatDock`, tout sélectionner, section entière, couper/coller vers une autre section, masquer/supprimer/dégrouper), reprise de l'écran et de la section au redémarrage (`useResume` dans `App.tsx`), aller à une section.
- **Sections** : sous-sections photo (`poseSections.ts`, `parentId`), masquer/réafficher, dupliquer (copie juste sous l'original, fichiers copiés), « Ordre du jour » en un clic (`src/dayOrder.ts`), progression par section (`src/progress.ts`, `SectionProgress.tsx` : poses + photos).
- **Packs** : import d'un pack de poses (`packImport.ts`), bibliothèque de poses (`library.ts`), pack photographe (« Mariage Andy.pdf ») et « shoot processé » (237 photos) importés dans le projet réel de l'app de bureau.
- **Références du couple (DaVinci Resolve)** — `src/refs.ts`, `screens/References.tsx`, `RefImport.tsx` : les 427 plans découpés de la timeline « maeva et andy » (6 vidéos Instagram choisies par le couple) sont des tâches (7 statuts, priorité Must Have/Important/Bonus/Optionnel, responsable, plan B, style de montage, intensité), avec progression globale/par étape/par priorité, viewer vidéo en boucle (aperçus de 480 px, sources jamais copiées), journal d'activité (`Project.activity`), « Je suis » (`localStorage visionnary-who`). Aperçu Jour J : bouton dans `MissionBoard` et `Home`.
- **Bibliothèque d'inspiration VISIONNARY** (`screens/InspirationLibrary.tsx`, route `#/bibliotheque/inspiration`) : 2569 plans de la timeline « Timeline 1 » (92 vidéos + 10 transferts), stockés dans le projet masqué « Bibliothèque de poses » avec `refSource: "library"` ; « Ajouter au mariage » copie le plan (le retirer du mariage ne touche jamais la bibliothèque). Les références du couple (`refSource: "couple"`) sont exclues de `itemsOf(p, "inspirations")` : jamais mélangées.
- **Manifestes Resolve** (schéma `type: couple_reference | inspiration_library`) dans `~/Downloads/24 Septembre - REFERENCES COUPLE (Resolve)` et `... BIBLIOTHEQUE INSPIRATION (Resolve)` (manifest.json + clips/). Lecture seule sur Resolve (aucun montage modifié ; seule la timeline courante a été changée puis remise sur « Timeline 1 »).
- **Équipe** créée dans le projet réel : Trude (photographe, Sony A7, 35-150 mm + 24-70 mm cérémonie), Mauricet (réalisation), Serge, Jole.
- **Limites connues / à faire** : la synchro temps réel multi-appareils n'existe pas (le serveur `server/` est local et optionnel, la synchro reste par sauvegarde/restauration) ; pas de liens partageables ni de rôles ; classement automatique des 2569 plans fait par lecture visuelle de planches (à valider, confiance « moyenne ») ; le programme du mariage (PDF) n'a pas été fourni (le PDF reçu est la liste de poses du photographe, déjà importée) ; alerte de fin d'étape = bouton « Fin d'étape » dans Références (pas encore déclenchée à la sortie d'étape du Mode Jour J).

## 24–26/09/2026 — Suite de session : mise en ligne, synchro, tablette
- **Site public (GitHub Pages)** : https://mariageafro.github.io/visionnary-app/ — déployé à chaque push sur `main`. Données Andy & Maeva embarquées dans `public/seed/andy-maeva-7k2q/` (`plan.json` + vignettes) ; chargées par `src/seedOnline.ts` via les liens `#/charger/andy-maeva` (vidéo) et `#/charger/andy-maeva/photo` (photo, 229 poses = export de l'app de bureau SANS les poses/sections masquées).
- **Synchro sans compte** (`src/liveSync.ts`) : canal ntfy.sh secret (`Project.syncTopic`), dernière écriture gagnante par clé (`pose:<id d'origine>` / `ref:<code>`), messages conservés 12 h, republication au démarrage. Projet de l'app de bureau relié au même canal. Test à deux appareils dans `tests/liveSync.test.ts`.
- **Références du couple** : deux jeux (`refSet` = `teaser` 367 plans en 28 missions, `resolve` 427 plans). Dossiers triés sur le Mac : `~/Movies/MAEVA & ANDY - CLIPS TRIES PAR MISSION` (+ `00_FEUILLE_DE_ROUTE.html`). Import desktop : `~/Downloads/24 Septembre - PLAN VIDEO TEASER (367)` (manifest `set: "teaser"`).
- **Fichiers AirDrop hors ligne** : `~/Desktop/POUR LA TABLETTE (AirDrop)/` (galerie photo avec visionneuse d'angles, plan vidéo, feuille de route). Page publique statique : `/photographe-andy-maeva-7k2q/`.
- **Tablette : problème NON résolu** — sur la tablette de l'utilisateur les photos ne s'affichent pas (téléphone OK). Correctifs déjà poussés : polyfills (`src/polyfills.ts`), décodage image sans `createImageBitmap` (`src/media.ts`), repli sans `aspect-ratio` (`src/screens/legacy.css`), page de diagnostic `/diag.html` à faire ouvrir sur la tablette (capture demandée). En attente de la capture / modèle d'iPad.
- **Hors dépôt / à ne pas commiter** : `src/andyMaevaWedding.ts`, changements de `Welcome.tsx`/`Timeline.tsx` d'un autre outil (copies dans `~/Downloads/autre-outil-non-commite`), `desktop/`, `release/`. Le dépôt est PUBLIC : ne jamais y pousser de données privées.
- **Reste** : diagnostic tablette ; masquer/supprimer en ligne se fait en republiant `plan.json` depuis l'app de bureau (script d'export via CDP, port 9333) ; Gâteau/flashmob quasi sans références ; classement des 367 + 2569 plans fait à l'œil (à valider).

## 26/09/2026 — Galerie photographe en ligne identique à l'app
- Le site charge maintenant **toutes** les poses de l'app de bureau : 266 visibles, 44 masquées (filtre « Masquées »), **32 favoris ★**, section « Choix de Maeva ★ » (10), séries d'angles, 2 sections masquées. Données dans `public/seed/andy-maeva-7k2q/poses.json` + `ph/` (513 photos), chargées par `src/seedOnline.ts` (marqueur `seedV: "3"` ; les anciens chargements sont remplacés).
- Republier après un changement dans l'app : `scripts/export-poses.mjs` (voir l'en-tête), puis commit + push. Les visiteurs qui ont déjà chargé l'ancienne version doivent rouvrir `#/charger/andy-maeva/photo`.
